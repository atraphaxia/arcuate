---
- name: Check if coreutils is installed
  community.general.pacman:
    name: coreutils
    state: installed

- name: Set timezone
  community.general.timezone:
    name: "{{ timezone }}"
    hwclock: UTC
